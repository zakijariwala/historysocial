# Sources stay local. They are never deployed and never committed.
sources/*
!sources/manifest.yaml
!sources/README.md

# Rendered output. PNGs are never committed.
out/
site/posts/
site/index.json
*.zip

# Cover source images ARE committed: they are inputs, and the render job in
# Actions must not depend on a museum API being up. Rendered slides are not.

# Scratch database for cover-variant work. Not truth.
scratch.db
scratch.db-wal
scratch.db-shm

__pycache__/
*.pyc
.venv/
.DS_Store
